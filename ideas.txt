strategy:

only works on nytimes.com (case insensitive)

let site fully load

run javascript that searches document.getblah by id 
  document.get blah by class


deleting id="gateway-content" 
deleting class="css-gx5sib"
change div class = css-mcm29f position:fixed -> position: relative