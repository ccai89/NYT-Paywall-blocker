function ready() {
  setTimeout(() => {
    document.getElementById('gateway-content').remove();
    setTimeout(document.querySelector('.css-mcm29f').setAttribute('style', 'position:relative'),500);
    setTimeout(document.querySelector('.css-gx5sib').setAttribute('style', 'background: transparent'),500);
  },1000);
}

document.addEventListener('DOMContentLoaded', ready);